SUPABASE_URL=
SUPABASE_ANON_KEY=
# Required for seed script (bypasses RLS). Get from Supabase Dashboard → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=
# Admin login password. Set in Supabase Edge Function secrets (Dashboard → Project Settings → Edge Functions)
ADMIN_PASSWORD=
